{
  "name": "fashion-sales",
  "version": "1.0.0",
  "description": "FASHION. — Curated sales dashboard",
  "private": true,
  "scripts": {
    "fetch-images": "node scripts/fetch-images.js",
    "fetch-images:dry": "node scripts/fetch-images.js --dry-run",
    "fetch-images:force": "node scripts/fetch-images.js --force --verbose",
    "validate": "node scripts/validate-picks.js",
    "setup-r2": "node scripts/setup-r2.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "dotenv": "^16.4.0"
  }
}
