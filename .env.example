# ===== Cloudflare R2 Configuration =====
# Copy this file to .env and fill in your values
#
# 1. Go to Cloudflare Dashboard → R2 Object Storage
# 2. Create a bucket (e.g., "fashion-images")
# 3. Go to R2 → Overview → Manage R2 API Tokens → Create API Token
# 4. Set permissions: Object Read & Write
# 5. Copy the credentials below
# 6. Set up a custom domain or use the R2.dev subdomain for public access
#    → In your bucket settings → Settings → Public Access → Enable R2.dev subdomain

# Your Cloudflare Account ID (found in dashboard URL or R2 overview)
R2_ACCOUNT_ID=your_account_id_here

# R2 API Token credentials
R2_ACCESS_KEY_ID=your_access_key_id_here
R2_SECRET_ACCESS_KEY=your_secret_access_key_here

# Your R2 bucket name
R2_BUCKET_NAME=fashion-images

# Public URL prefix for your bucket
# Option A: R2.dev subdomain (free, auto-generated)
#   e.g., https://pub-abc123def456.r2.dev
# Option B: Custom domain via Cloudflare
#   e.g., https://images.yoursite.com
R2_PUBLIC_URL=https://pub-xxxxxxxxxxxx.r2.dev
